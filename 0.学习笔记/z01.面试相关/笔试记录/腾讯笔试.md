```
1. 给定长度为N的A,B两个数组，B为权值数组，范围是【0-2】，现在要求构建C数组，满足以下要求：
	+ 长度为N，范围是【1-N】且无重复元素，即为N的一个全排列
	+ 当i < j, B[i] > B[j]时， 需要满足C[i] > C[j];
	+ 使得A与C的差的绝对值x最小，求这个x值
2. 给定数组A，求满足异或值=乘积值的子序列（不要求连续）的数量
3. 给定数组A，和一个k值，求最大公约数是k的子序列（不要求连续）数量
``` 
