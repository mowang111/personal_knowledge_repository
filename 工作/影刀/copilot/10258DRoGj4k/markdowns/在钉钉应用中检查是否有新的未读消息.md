---
tags:
---
{'err_funcs': ['xbot_visual.desktop.contains_string']}
```python
# 打开钉钉
process_info=xbot_visual.desktop.launch(process_name="钉钉")
# 检查是否有新的未读消息
if xbot_visual.desktop.contains_string(window="0", element="新消息提醒", text="新消息"):
    # 如果有新的未读消息，打印提醒
    print("您有新的未读消息！")
# 否则
else:
    # 如果没有新的未读消息，打印提醒
    print("没有新的未读消息")
```
---
```python
# 打开钉钉
process_info=xbot_visual.desktop.launch(process_name="钉钉")
# 获取未读消息数量
unread_message_count = xbot_visual.desktop.get_attribute(window="0", element="钉钉未读消息", attribute_name="text")
# 判断是否有未读消息
if unread_message_count != "0":
    # 点击进入消息列表
    xbot_visual.desktop.click(window="0", element="钉钉消息列表")
    # 获取第一条未读消息对象
    unread_message = xbot_visual.desktop.find_element(window="0", element="钉钉未读消息元素")
    # 获取未读消息内容
    unread_message_text = xbot_visual.desktop.get_attribute(window="0", element=unread_message, attribute_name="text")
```
